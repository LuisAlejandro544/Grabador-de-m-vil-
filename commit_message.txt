fix(ci): corregir compilación Kotlin en GitHub Actions y remover dependencia de .env

- Eliminar el plugin Secrets Gradle y la necesidad de archivos `.env`/`.env.example`, permitiendo que la aplicación se compile de forma 100% autónoma y autosuficiente sin requerir claves de terceros.
- Configurar `android-actions/setup-android` y la aceptación automática de licencias de Android SDK (`platforms;android-36` y `build-tools;36.0.0`) en el flujo de CI.
- Desactivar `configuration-cache` en el entorno de CI (`--no-configuration-cache`) y ajustar las opciones JVM de Gradle para prevenir excepciones en `GradleKotlinCompilerWorkAction` y fallos en trabajadores del compilador Kotlin.
- Sincronizar la generación del Gradle Wrapper con la versión 9.3.1 especificada en `gradle-wrapper.properties`.
- Resolver advertencias y compatibilidad en `ScreenRecordService` (`getParcelableExtra` adaptado a API 33+) y `SettingsView` (`Icons.AutoMirrored.Filled.OpenInNew`).
