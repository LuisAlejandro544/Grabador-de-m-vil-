feat(native-core & ffmpeg): cimientos de FFmpeg puro nativo (libav*), compositor gráfico multicapa C++ y widget flotante

- Añadir estructura base de FFmpeg puro nativo en C/C++ (`ffmpeg_engine.hpp` / `ffmpeg_engine.cpp`) sin dependencias descontinuadas (FFmpegKit).
- Definir interfaces de procesamiento multimedia para recorte de video (stream copy / frame accurate), extracción de audio (AAC/MP3), marcas de agua y compresión.
- Implementar puente JNI y adaptador en Kotlin (`NativeFFmpegBridge.kt`) con fallback seguro contra `UnsatisfiedLinkError`.
- Integrar información y versión del motor FFmpeg puro en `SettingsView.kt`.
- Implementar motor gráfico de composición multicapa en C++ (`obs_compositor.cpp` / `obs_compositor.hpp`) con shaders GLSL de vértice y fragmento (máscara circular Facecam y Chroma Key GPU).
- Configurar inicialización de contexto EGL, buffers VBO/VAO y ordenamiento de profundidad de capas (*z-order*).
- Implementar `FloatingBubbleManager` con `WindowManager` para widget flotante con cronómetro en vivo y controles interactivos sobre juegos.
- Actualizar documentación arquitectónica (`README.md`, `ROADMAP.md`, `STRUCTURE.md` y `AI_CONTEXT.md`).


