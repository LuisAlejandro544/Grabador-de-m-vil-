feat: modo para ocultar la burbuja flotante en el video final con soporte Android 14+ y versiones anteriores

- Implementación del modo "Ocultar burbuja en el video final":
  * En Android 14+ (API 34+), exclusión nativa de la vista de superposición mediante flags de ventana (`FLAG_SECURE` / exclusión de `VirtualDisplay`), permitiendo que el usuario vea la burbuja mientras juega pero no aparezca grabada en el video final.
  * En versiones anteriores a Android 14, atenuación y minimización visual automática durante la captura para evitar intrusiones en el gameplay grabado.
  * Adición de la opción conmutable en la configuración de la burbuja flotante (`hideBubbleInFinalVideo`), integrada en `RecordingConfig`, `OverlaySettingsStore`, `SettingsRepository`, `RecordViewModel`, `SettingsView` y `FloatingBubbleSettingsCard`.
  * Integración en el servicio de grabación en primer plano (`ScreenRecordService`, `ServiceOverlayCoordinator`, `FloatingBubbleManager` y `ServiceActionDispatcher`).
