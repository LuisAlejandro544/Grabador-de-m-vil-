refactor(modularization): desacoplar ScreenRecordService, HomeScreen y FloatingBubbleManager en módulos especializados

- Desacoplar `ScreenRecordService` extrayendo `ScreenCaptureEngine` (gestión de MediaProjection, VirtualDisplay y MediaRecorder), `RecordNotificationHelper` (canales y notificaciones persistentes con acciones de pausa/reanudación) y `RecordStorageHelper` (rutas de guardado y escaneo en MediaStore).
- Modularizar `FloatingBubbleManager` dividiendo la lógica de arrastre en `BubbleTouchHandler` y la jerarquía visual con cronómetro y pulso animado en `BubbleOverlayView`.
- Refactorizar `HomeScreen` en componentes limpios e independientes: `RecordTopBar`, `RecordBottomBar`, `RecordTab` y `GalleryTab`.
- Añadir submenú interactivo "Herramientas" en el widget flotante con soporte para Captura de Pantalla rápida (`ScreenshotHelper`) y Lienzo de dibujo/pincel en tiempo real (`ScreenDrawingOverlay`) con selector de colores, grosores y borrado rápido sobre grabaciones activas.
- Añadir pruebas unitarias Robolectric para validar el comportamiento del asistente de almacenamiento, captura de instantáneas y widget flotante.
- Actualizar `STRUCTURE.md`, `README.md`, `ROADMAP.md` y `AI_CONTEXT.md` reflejando las nuevas herramientas y arquitectura modular del proyecto.
