feat(ci): agregar GitHub Action Foundation y reglas de exportación limpia (.gitattributes)

- Creación del Workflow de Scaffolding Foundation (`.github/workflows/foundation.yml`):
  * Automatiza la inicialización cuando un usuario crea un nuevo repositorio o fork a partir de la plantilla original (https://github.com/LuisAlejandro544/Vortex).
  * Incluye validación de seguridad condicional (`if: github.repository != 'LuisAlejandro544/Vortex'`) para proteger el repositorio plantilla principal y activarse únicamente en repositorios derivados.
  * Personaliza automáticamente referencias y enlaces en la documentación hacia el nuevo propietario del repositorio.
  * Implementa auto-eliminación segura del propio script de inicialización tras la primera ejecución, dejando el árbol de código fuente limpio.

- Configuración de Empaquetado Limpio (`.gitattributes`):
  * Integra directivas `export-ignore` para omitir la carpeta `.github/`, workflows internos de CI/CD y metadatos del repositorio cuando un usuario descarga el código en formato ZIP (*Download ZIP*) o mediante `git archive`.
