feat(facecam): implementar camara flotante con multiples disenos geometricos y conmutacion desde la burbuja de grabacion

- Integrar módulo de Facecam flotante (`FacecamOverlayManager`) mediante CameraX y `WindowManager` con ciclo de vida desacoplado (`FacecamLifecycleOwner`) para renderizado fluido en segundo plano.
- Añadir soporte para múltiples diseños y formas geométricas aceleradas por hardware: Circular 1:1, Cuadrado Redondeado Moderno, Cuadrado 1:1 y Rectangular Panorámico 16:9 estilo webcam.
- Implementar control de tamaños configurables (Pequeño 100dp, Mediano 140dp, Grande 180dp) y alternancia instantánea entre lente frontal (selfie) y lente trasero.
- Integrar botón de conmutación rápida de Facecam (`Activar / Desactivar Cámara`) dentro del submenú de herramientas de la burbuja flotante (`BubbleOverlayView` y `FloatingBubbleManager`).
- Ampliar `RecordingConfig` y `SettingsRepository` para persistir las preferencias de Facecam (estado activo, forma geométrica, tamaño y selección de cámara).
- Añadir sección dedicada de configuración visual de Facecam en `SettingsView` con selectores interactivos y gestión dinámica del permiso `android.permission.CAMERA`.
- Declarar permisos de cámara y tipo `FOREGROUND_SERVICE_TYPE_CAMERA` en `AndroidManifest.xml` y `ScreenRecordService` para compatibilidad con Android 14+.
- Actualizar la documentación técnica del proyecto (`README.md`, `ROADMAP.md`, `STRUCTURE.md` y `AI_CONTEXT.md`).
